/**
 * frame模块url配置
 */
var url = {
    login: urlBase + '/auth-center/auth/login.json', //登陆接口
    navData: urlBase + '/webapi-container/menu/availabe.json', //获取菜单接口
};

//本地模拟数据
var unRealUrl = {
    navData: '../../data/nav.json'
};
